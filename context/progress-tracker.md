# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature 10 (Done)

## Current Goal

- Ready for the next feature unit.

## Completed

- Feature 01: Design System — shadcn/ui installed and initialized (Tailwind v4), all 7 components added (Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea), lucide-react installed, lib/utils.ts with cn() created, globals.css updated with full dark theme palette (--bg-base through --state-warning), shadcn semantic variables mapped to design system tokens, dark class applied to html element.
- Feature 02: Editor Chrome — `components/editor/editor-navbar.tsx` (fixed-height top bar with left/center/right sections and `PanelLeftOpen`/`PanelLeftClose` toggle), `components/editor/project-sidebar.tsx` (floating overlay with slide-in animation, My Projects / Shared tabs with empty placeholders, full-width New Project button with `Plus` icon), `components/editor/editor-dialog.tsx` (reusable dialog wrapper with title/description/footer slots using design tokens). Added missing `--color-base` token to `app/globals.css` so the documented `bg-base` utility resolves.
- Feature 03: Auth — installed `@clerk/ui`; wrapped `app/layout.tsx` with `ClerkProvider` using the `dark` theme from `@clerk/ui/themes`, overriding Clerk appearance variables with the app's CSS custom properties (no hardcoded colors); created `proxy.ts` at the project root using `clerkMiddleware` + `createRouteMatcher`, public matcher derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars, all other routes protected via `auth.protect()`; updated `app/page.tsx` to redirect authenticated users to `/editor` and unauthenticated users to `/sign-in`; added Clerk's `<UserButton>` to the right section of `components/editor/editor-navbar.tsx`. Added `NEXT_PUBLIC_CLERK_SIGN_IN_URL` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL` to `.env.local`.
- Route-group restructure — split `app/` into `(authenticated)` and `(unauthenticated)` route groups (URLs unchanged). `(authenticated)/layout.tsx` mounts `components/editor/editor-shell.tsx` (new client wrapper that owns sidebar open/close state and assembles `EditorNavbar` + `ProjectSidebar` around `{children}`); `(authenticated)/editor/page.tsx` renders the placeholder inside that shell. `(unauthenticated)/layout.tsx` owns the two-panel grid + `AuthMarketingPanel`; `sign-in` and `sign-up` pages collapse to one-line `<SignIn />` / `<SignUp />` renders. `app/page.tsx` (the `/` redirect) stays at root. `npm run build` passes — routes resolve to `/`, `/editor`, `/sign-in/[[...sign-in]]`, `/sign-up/[[...sign-up]]`.
- Feature 04: Project Dialogs & Editor Home — added shadcn `dropdown-menu` (`components/ui/dropdown-menu.tsx`); `lib/slugify.ts` (NFKD + diacritic strip → lowercase kebab); `lib/mock-projects.ts` exports `MockProject` type and a 3-row `MOCK_PROJECTS` seed (2 owned, 1 shared); `hooks/use-project-dialogs.ts` is a single source of truth for dialog kind / target / loading / submit handlers (mock submits fake a 250ms delay then close); `components/editor/project-dialogs-context.tsx` exposes `openCreate`/`openRename`/`openDelete` to descendants; `components/editor/editor-home.tsx` (client) renders the `/editor` heading, description, and `New Project` button wired to context; `app/(authenticated)/editor/page.tsx` collapses to a single `<EditorHome />`; `components/editor/dialogs/{create,rename,delete}-project-dialog.tsx` mount/unmount via parent (state resets on remount, no setState-in-effect); Create has live slug preview; Rename auto-focuses + selects via stable ref callback and Enter submits via `<form>`; Delete has destructive-only confirm. `components/editor/project-sidebar.tsx` now lists projects per tab with hover-revealed `MoreHorizontal` dropdown (Rename + destructive Delete) for owned projects only, and a `md:hidden` backdrop scrim that closes the sidebar on tap. `components/editor/editor-shell.tsx` owns the dialog hook, provides the context, passes mock data + handlers into the sidebar, and conditionally mounts each dialog. `npm run lint` and `npm run build` pass.
- Feature 05: Prisma Schema & Data Layer — `prisma/models/project.prisma` defines `ProjectStatus` enum (`DRAFT`, `ARCHIVED`), `Project` (cuid id, `ownerId`, `name`, optional `description`, `status` defaulting to `DRAFT`, optional `canvasJsonPath`, `createdAt`/`updatedAt`, indexes on `ownerId` and `createdAt`), and `ProjectCollaborator` (cuid id, `projectId`, `email`, `createdAt`, cascade-delete relation back to `Project`, `@@unique([projectId, email])`, indexes on `email` and `[projectId, createdAt]`). Installed `@prisma/extension-accelerate`. `lib/prisma.ts` is a cached singleton: when `DATABASE_URL` starts with `prisma+postgres://` it instantiates `new PrismaClient({ accelerateUrl }).$extends(withAccelerate())`, otherwise it wires `new PrismaClient({ adapter: new PrismaPg({ connectionString }) })`. The instance is stored on `globalThis.prisma` outside production. Initial migration `prisma/migrations/20260503112540_init/migration.sql` applied via `npx prisma migrate dev --name init`; client generated to `app/generated/prisma/` (already gitignored). `npm run build` passes.
- Feature 06: Project APIs — `app/api/projects/route.ts` exports `GET` (returns `{ projects }` for the Clerk-authenticated user, ordered by `createdAt desc`) and `POST` (creates a project with `ownerId = userId`, defaults missing/blank `name` to `"Untitled Project"`, accepts optional trimmed `description`, returns `201` with `{ project }`). `app/api/projects/[projectId]/route.ts` exports `PATCH` (rename via `{ name }` body — owner-only, `400` on missing/blank name or invalid JSON, `404` when project not found, `403` for non-owners) and `DELETE` (owner-only, `404`/`403` mirroring PATCH, `204` on success). Both routes call `auth()` from `@clerk/nextjs/server` and return JSON `401` when `userId` is null. Updated `proxy.ts` to skip `auth.protect()` for `/api/*` paths so handlers can return JSON `401` instead of the `404` Clerk emits for non-document requests; document routes still go through `auth.protect()`. Tightened `lib/prisma.ts` return type from the `PrismaClient | ExtendedPrismaClient` union (which collapsed model accessors to `unknown`) to `PrismaClient`, casting the Accelerate-extended branch via `as unknown as PrismaClient`. `npm run build` and `npm run lint` pass; routes appear as `/api/projects` and `/api/projects/[projectId]` in the build output.
- Feature 07: Wire Editor Home — added `lib/projects.ts` server-side data helper exposing `ProjectListItem` (`{ id, name, ownership }`) and `getUserProjects()` which uses Clerk `auth()` + `currentUser()` to fetch owned projects (`ownerId = userId`) and shared projects (other users' projects where any of the user's email addresses appear in `ProjectCollaborator`). `app/(authenticated)/layout.tsx` is now async — fetches `{ owned, shared }` and passes both into `EditorShell` props (no client-side fetching for initial load). Created `hooks/use-project-actions.ts` (replaces `use-project-dialogs.ts`) — single source of truth for dialog kind + loading + create/rename name inputs + delete target; generates a 4-char base36 `createSuffix` on `openCreate` and exposes `createRoomId = ${slug(name)}-${suffix}` so the project ID and Liveblocks room ID stay aligned. `submitCreate` POSTs `{ id: roomId, name }` to `/api/projects`, then `router.push(/editor/${id})`; `submitRename` PATCHes name and `router.refresh()`; `submitDelete` DELETEs and either `router.push("/editor")` if `usePathname()` matches the active workspace or `router.refresh()` otherwise. Updated `POST /api/projects` to accept an optional trimmed `id` in the body (passes through to Prisma so the cuid default is overridden). Dialogs are now controlled — `CreateProjectDialog` shows a "Room ID preview" label sourced from the hook; `RenameProjectDialog` pre-fills via lifted state and `onNameChange`; `DeleteProjectDialog` shows the project name. `ProjectSidebar` now takes split `ownedProjects` / `sharedProjects` props of `ProjectListItem` and shows `project.id` (the slug+suffix) in mono under the name. Deleted `lib/mock-projects.ts` and `hooks/use-project-dialogs.ts`. `npm run lint` and `npm run build` pass.
- Feature 08: Editor Workspace Shell — `lib/project-access.ts` exports `getCurrentIdentity()` (`{ userId, primaryEmail, emails[] }` via Clerk `auth()` + `currentUser()`; null when unauthenticated) and `checkProjectAccess(projectId, identity)` (returns `{ id, name, ownership }` or `null`; matches owner by `ownerId`, collaborator by case-insensitive `ProjectCollaborator.email` against any of the user's verified emails — consistent with the `getUserProjects()` shared-list query). `components/editor/access-denied.tsx` is a server component with a centered layout, lock icon, short message, and a `<Link>` styled via `buttonVariants({ variant: "outline" })` back to `/editor`. New server route `app/(authenticated)/editor/[roomId]/page.tsx` redirects unauthenticated users to `/sign-in`, then runs `checkProjectAccess` + `getUserProjects` in parallel; on miss/deny mounts `<EditorShell>` with `<AccessDenied />`, on hit mounts `<EditorShell currentProject={...}>` wrapping a centered canvas placeholder. `EditorShell` gained an optional `currentProject` prop and now owns `isAiOpen` state — when set, it forwards the project name + share button + AI toggle into `EditorNavbar`, passes `currentProjectId` into `ProjectSidebar`, and renders `<AiSidebar />` (right-side floating overlay placeholder). `EditorNavbar` shows the project name next to the sidebar toggle and conditionally renders a `Share2` outline button + `Sparkles` AI toggle. `ProjectSidebar` rows are now `<Link href={/editor/${id}}>` with `aria-current="page"` and a `bg-brand-dim` + `text-brand` highlight for the current project; the dropdown trigger sits as a sibling outside the link so it never triggers navigation. To accommodate per-page workspace chrome, `(authenticated)/layout.tsx` collapsed to a passthrough and `(authenticated)/editor/page.tsx` + `(authenticated)/editor/[roomId]/page.tsx` each fetch projects and mount `EditorShell` themselves. `npm run lint` and `npm run build` pass; `/editor/[roomId]` appears in the route manifest.
- Feature 09: Share Dialog — `lib/clerk-users.ts` exposes `enrichCollaborators(rows)` which calls `clerkClient().users.getUserList({ emailAddress, limit })` (Clerk Backend API) once per dialog open and joins the resulting users back onto the collaborator rows by lowercased email; returns `{ id, email, displayName, imageUrl }`, with `displayName = "First Last" || username || null` and `imageUrl = user.imageUrl || null`. Lookup soft-fails (try/catch) so a Clerk outage degrades to email-only display. `app/api/projects/[projectId]/collaborators/route.ts` exports `GET` (owner OR collaborator via `checkProjectAccess` — returns `{ collaborators }` enriched, ordered by `createdAt asc`, `404` on miss/deny) and `POST` (owner-only `{ email }` invite — `400` on missing/invalid email, `404` when project not found, `403` for non-owners, `409` on duplicate via Prisma `P2002`, `201` `{ collaborator }` enriched). Email is validated against `^[^\s@]+@[^\s@]+\.[^\s@]+$` and stored lowercased. `app/api/projects/[projectId]/collaborators/[collaboratorId]/route.ts` exports `DELETE` (owner-only — `404`/`403` mirror POST, `204` on success; cuid `id`-scoped within the project so a wrong project URL won't remove cross-project rows). `components/editor/dialogs/share-dialog.tsx` is a controlled client dialog with three sections: an owner-only invite form (email input + Invite button + inline error), a collaborator list (avatar via `imageUrl` or initial fallback, displayName + email, owner-only `Trash2` remove button), and an owner-only copy-link block (`/editor/${projectId}` mono preview + outline button that copies `${window.location.origin}/editor/${projectId}` and flips to `Check` + `Copied!` for 2s). Initial fetch runs in `useEffect` with an `AbortController`; state mutations live in `.then`/`.catch` (no synchronous setState in the effect body). `EditorShellCurrentProject` now carries `ownership: ProjectOwnership`; `EditorShell` owns `isShareOpen` state, swaps the navbar's `showShare` boolean for an `onOpenShare` callback prop, and conditionally mounts `<ShareDialog>` (passing `isOwner={ownership === "owned"}`) so the dialog state is recreated each open. `(authenticated)/editor/[roomId]/page.tsx` forwards `access.ownership` into `currentProject`. `npm run lint` and `npm run build` pass; new routes `/api/projects/[projectId]/collaborators` and `/api/projects/[projectId]/collaborators/[collaboratorId]` appear in the route manifest.
- Feature 10: Liveblocks Setup — installed `@liveblocks/node`. `liveblocks.config.ts` declares the global `Liveblocks` interface with `Presence` (`cursor: { x, y } | null` + `isThinking: boolean`) and `UserMeta` (`id` + `info: { name, avatar?, color }`). `lib/liveblocks.ts` exports `getLiveblocksClient()` — a lazily-instantiated cached singleton stored on `globalThis.liveblocks` (only outside production), constructing `new Liveblocks({ secret: process.env.LIVEBLOCKS_SECRET_KEY! })` on first call so `next build` page-data collection doesn't trip the missing-env check at module load. Same file exports `getCursorColor(userId)` which deterministically maps a user ID to one of an 8-color palette (`#52A8FF`, `#BF7AF0`, `#FF990A`, `#FF6166`, `#F75F8F`, `#62C073`, `#0AC7B4`, `#FBBF24`) via a 31-multiplier rolling-int hash. `app/api/liveblocks-auth/route.ts` (`POST`) reads `{ room }` from the JSON body, requires Clerk auth via `getCurrentIdentity()` (returns JSON `401` when null), then `checkProjectAccess(room, identity)` — returning `403` for non-owner/non-collaborator. On access hit, it calls `liveblocks.getOrCreateRoom(access.id, { defaultAccesses: [], metadata: { name } })` (private; we gate at issuance), then issues an access token via `liveblocks.prepareSession(userId, { userInfo: { name, color, ...(avatar ? { avatar } : {}) } })` + `session.allow(access.id, FULL_ACCESS)` + `session.authorize()`. `name` resolves through `currentUser()` first/last/username then falls back to primary email then user ID; `avatar` comes from `user.imageUrl` (omitted when missing because Liveblocks `IUserInfo.avatar` is typed as `string`, not `string | null`); `color` comes from `getCursorColor(userId)`. `LiveblocksError` from `getOrCreateRoom` returns `502`. `npm run lint` and `npm run build` pass; new route `/api/liveblocks-auth` appears in the route manifest.

## In Progress

- None.

## Next Up

- See feature-specs directory for the next unit.
- Liveblocks room mount on the canvas surface; real canvas surface; AI chat content.

## Open Questions

- None.

## Architecture Decisions

- Using shadcn/ui on Tailwind v4 with CSS-variable-based dark theme (dark only, no light mode).
- All color tokens defined as CSS custom properties in globals.css; shadcn semantic vars (--background, --primary, etc.) point to design system vars (--bg-base, --accent-primary, etc.).
- @theme inline maps design system tokens to Tailwind utilities: bg-surface, text-copy-primary, border-surface-border, text-brand, bg-brand-dim, etc.
- html element carries class="dark" so shadcn dark: variants always activate.
- Auth uses Clerk via `@clerk/nextjs` and `@clerk/ui`; route protection lives in `proxy.ts` at the project root (Next.js 16 file-convention rename of `middleware.ts`). Protected-by-default, with sign-in / sign-up paths derived from `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` exposed as public.
- `ClerkProvider` lives inside `<body>` in `app/layout.tsx` (required by `@clerk/nextjs` v7); appearance overrides reference the app's existing CSS variables — no hardcoded Clerk colors.
- Prisma 7 with the `prisma-client` generator outputting to `app/generated/prisma/` (gitignored). Schema is multi-file: top-level `prisma/schema.prisma` holds the generator + datasource, model files live under `prisma/models/`. Database access goes through the cached `lib/prisma.ts` singleton, which switches between Accelerate (`prisma+postgres://`) and the direct `@prisma/adapter-pg` driver based on the `DATABASE_URL` scheme. `prisma.config.ts` loads `.env` via `dotenv/config` and points `schema` at `prisma/`. The singleton is typed as `PrismaClient` (Accelerate-extended branch is cast through `unknown`) so model accessors stay strongly typed at call sites.
- API auth lives inside route handlers via `auth()` from `@clerk/nextjs/server`, returning JSON `401` when `userId` is null. `proxy.ts` short-circuits before `auth.protect()` for any `/api/*` path so API requests get the JSON `401` contract instead of Clerk's default `404` for non-document requests; document routes still go through `auth.protect()`. Mutation handlers fetch the target row, return `404` if missing and `403` if `ownerId !== userId` before applying the change.
- Editor sidebar data is fetched server-side. Each editor page (`(authenticated)/editor/page.tsx` and `(authenticated)/editor/[roomId]/page.tsx`) is an async server component that calls `getUserProjects()` from `lib/projects.ts` and passes `ownedProjects` / `sharedProjects` into `EditorShell`. The route-group layout `(authenticated)/layout.tsx` is a thin passthrough so the workspace shell mounts per-page (the layout cannot see `[roomId]` params, and the workspace navbar needs to show the current project's name). No client-side fetch for initial load — refresh comes from `router.refresh()` after a mutation, or `router.push()` after create/active-workspace delete.
- Project access checks happen in the page server component, not in middleware. `lib/project-access.ts` exposes `getCurrentIdentity()` (Clerk `auth()` + `currentUser()`) and `checkProjectAccess(projectId, identity)` (Prisma owner-or-collaborator match against any of the user's verified emails). The workspace page redirects unauthenticated users to `/sign-in`, and on access miss/deny mounts `<AccessDenied />` inside the `EditorShell` so the project sidebar stays available for navigation. The shell shows workspace-only chrome (project name in navbar, share button, AI sidebar + toggle) only when `currentProject` is provided.
- Collaborator identity is sourced from Clerk via the Backend API at request time. `ProjectCollaborator` rows store only the lowercased email; `lib/clerk-users.ts` enriches a row set on demand via `clerkClient().users.getUserList({ emailAddress })` and joins on lowercased email. There is no local user table — Clerk is the source of truth for display name and avatar, and the helper soft-fails to email-only when the lookup errors. Collaborator-list endpoints (`GET /api/projects/[projectId]/collaborators`) authorize via `checkProjectAccess` (owner OR collaborator); mutating endpoints (`POST` invite, `DELETE` remove) authorize via the project's `ownerId === userId` and return `403` for non-owners.
- Project ID = Liveblocks room ID. The create flow generates `${slug(name)}-${4-char-base36-suffix}` client-side and sends it as the optional `id` field on `POST /api/projects` so the Prisma `Project.id` (otherwise `cuid()`) is overridden with that value. URLs are `/editor/[projectId]` where `projectId` is that same string.
- Shared-projects query keys on Clerk email. `getUserProjects()` reads all `emailAddresses` from `currentUser()` and joins on `ProjectCollaborator.email IN (...)`, scoped to projects whose `ownerId !== userId` so an owner doesn't double-appear in the Shared tab.
- Liveblocks auth uses **access tokens** (`prepareSession` + `session.allow(roomId, FULL_ACCESS)`) rather than ID tokens. Project membership is enforced once at the auth route via `checkProjectAccess`, so rooms are created with `defaultAccesses: []` (private) and the access scope rides on the token itself — no per-user `usersAccesses` bookkeeping on the room. The Liveblocks node client is exposed as `getLiveblocksClient()` (lazy + cached on `globalThis`) instead of a module-level constant so the missing-env guard does not fire during `next build` page-data collection. `Liveblocks.UserMeta.info.avatar` is `string | undefined` (not `string | null`) because Liveblocks' core `IUserInfo` types `avatar` as `string`; the route omits the field entirely when Clerk has no `imageUrl`.

## Session Notes

- Do not modify components/ui/* after installation per ai-workflow-rules.md.
- Design system palette is defined in globals.css :root; all other files reference tokens, never raw hex values.
- Keep Clerk's default `<UserButton>` and built-in profile flows intact — do not rebuild Clerk internals.
- React 19 lint rule `react-hooks/set-state-in-effect` blocks `setState` inside `useEffect`. For dialogs that need fresh state per open, mount/unmount conditionally from the parent rather than syncing props → state in an effect.
- Editor pages must remain server-rendered above `EditorShell` so they can do the initial server-side `getUserProjects()` (and `checkProjectAccess()`) fetch. Don't add `"use client"` to `(authenticated)/editor/page.tsx` or `(authenticated)/editor/[roomId]/page.tsx`. `EditorShell` itself is the client boundary.
- Prisma client output (`app/generated/prisma/`) is gitignored and recreated by `prisma generate`; never import directly from `@prisma/client` — always go through `lib/prisma.ts` or `@/app/generated/prisma/client`.
- `prisma migrate dev` only writes/applies migrations; if you need the generated client right after, run `prisma generate` (or rely on `next build` which triggers it via the generator).
- Per the React 19 `react-hooks/set-state-in-effect` rule, do not call `setState` synchronously inside a `useEffect` body. For dialogs whose state should reset on each open, mount/unmount the dialog conditionally from the parent and let initial state defaults handle the reset; state mutations from async handlers (then/catch) are fine.
