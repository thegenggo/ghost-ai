/**
 * Renders the editor page placeholder centered within a full-size container.
 *
 * @returns A React element containing a centered paragraph with the text "Editor placeholder".
 */
export default function EditorPage() {
  return (
    <div className="flex flex-1 items-center justify-center">
      <p className="text-copy-muted">Editor placeholder</p>
    </div>
  );
}
